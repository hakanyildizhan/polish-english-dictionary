<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".SearchResultRowFragment">

    <data>
        <variable
            name="viewModel"
            type="com.hakansoft.polishdictionary.SearchResultRowViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        >

        <TextView
            android:id="@+id/search_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{viewModel.rowTextString}"
            tools:text="1. kraj mi country, nation, land"
            android:textSize="18sp"
            android:fontFamily="@font/jura_regular"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="20dp"
            android:textColor="@color/black"/>

        <TextView
            android:id="@+id/textView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:fontFamily="@font/barlow_lightitalic"
            android:text="tap row to view grammar table"
            android:textColor="@color/red"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@+id/search_row"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="20dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/barlow_lightitalic"
            android:text="long tap to bookmark"
            android:textColor="@color/red"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@+id/textView"
            android:layout_marginLeft="20dp"
            android:layout_marginRight="20dp"
            android:layout_marginTop="-3dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>